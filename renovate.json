{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":pinDigests"
  ],
  "labels": ["dependencies"],
  "postUpdateOptions": ["gomodTidy"],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Bump the CI workflow's setup-go go-version in step with the toolchain, so a Go security patch clears govulncheck everywhere at once.",
      "fileMatch": ["^\\.github/workflows/[^/]+\\.ya?ml$"],
      "matchStrings": ["go-version:\\s*\"(?<currentValue>[0-9]+\\.[0-9]+\\.[0-9]+)\""],
      "depNameTemplate": "golang",
      "datasourceTemplate": "golang-version"
    }
  ],
  "packageRules": [
    {
      "description": "Auto-merge Go patch/security releases (go.mod toolchain, the CI go-version, and the Dockerfile base) and base-image digest refreshes. A red govulncheck otherwise blocks the publish job and stops all deploys, so these should not wait for a human.",
      "matchManagers": ["gomod", "dockerfile", "custom.regex"],
      "matchUpdateTypes": ["patch", "pin", "digest"],
      "automerge": true
    }
  ]
}
